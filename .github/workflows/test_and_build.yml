# This workflow has been replaced by ci.yml as part of the Go rebuild.
# Kept as an empty placeholder to avoid breaking any branch protection rules
# that reference it by name. Safe to delete once branch rules are updated.

name: Test and Build (deprecated)

on:
  workflow_dispatch: {}  # manual trigger only — no longer runs automatically

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. See ci.yml."
