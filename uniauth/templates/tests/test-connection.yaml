apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uniauth.fullname" . }}-test-connection"
  labels:
    {{- include "uniauth.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "uniauth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
